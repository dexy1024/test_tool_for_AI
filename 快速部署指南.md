# GitHub Pages 快速部署指南

## ✅ 已完成配置

所有配置文件已就绪，代码已提交到本地仓库。

## 🚀 推送代码（选择一种方法）

### 方法一：运行推送脚本（推荐）

```bash
cd /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition
./push-to-github.sh
```

### 方法二：使用 GitHub Desktop

1. 下载安装：https://desktop.github.com/
2. File → Add Local Repository
3. 选择项目文件夹
4. 点击 "Publish repository"

### 方法三：手动命令行推送

```bash
cd /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition

# 方法1：使用环境变量（推荐）
export GITHUB_TOKEN=your_token_here
git push https://${GITHUB_TOKEN}@github.com/dexy1024/test_tool_for_AI.git main

# 方法2：直接使用（不推荐，token 会出现在命令历史中）
git push https://YOUR_TOKEN@github.com/dexy1024/test_tool_for_AI.git main
```

## 📋 启用 GitHub Pages

推送代码后：

1. **访问仓库设置页面**
   ```
   https://github.com/dexy1024/test_tool_for_AI/settings/pages
   ```

2. **配置部署源**
   - 在 "Source" 部分
   - 选择 **"GitHub Actions"**（重要：不要选择 branch）
   - 点击 "Save"

3. **等待自动部署**
   - 查看部署状态：https://github.com/dexy1024/test_tool_for_AI/actions
   - 通常在 2-5 分钟内完成

4. **访问网站**
   ```
   https://dexy1024.github.io/test_tool_for_AI/
   ```

## 🔍 验证部署

- ✅ 部署成功：可以正常访问网站
- ❌ 部署失败：检查 Actions 日志中的错误信息

## 📝 注意事项

1. **首次部署可能需要几分钟**
2. **GitHub Actions 会自动构建**，不需要手动构建
3. **每次推送代码到 main 分支**，会自动触发重新部署
4. **dist 文件夹已在 .gitignore 中**，不会提交（GitHub Actions 会自动生成）

## 🌐 访问地址

部署成功后：
```
https://dexy1024.github.io/test_tool_for_AI/
```

---

**提示**：如果推送时遇到网络问题，可以：
- 检查网络连接
- 稍后重试
- 使用 GitHub Desktop（通常更稳定）


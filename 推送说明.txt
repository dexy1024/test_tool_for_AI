═══════════════════════════════════════════════════════════
    GitHub Pages 部署 - 推送说明
═══════════════════════════════════════════════════════════

✅ 已完成的工作：
───────────────────────────────────────────────────────────
1. ✅ 已配置 vite.config.js，base 路径为 /test_tool_for_AI/
2. ✅ 已创建 GitHub Actions 工作流
3. ✅ 已创建 .gitignore 文件
4. ✅ 已初始化 Git 仓库
5. ✅ 已提交所有代码到本地仓库

📤 现在需要推送代码到 GitHub：
───────────────────────────────────────────────────────────

方法一：使用 GitHub Desktop（最简单）⭐
───────────────────────────────────────────────────────────
1. 下载安装 GitHub Desktop：
   https://desktop.github.com/

2. 打开 GitHub Desktop
3. File → Add Local Repository
4. 选择文件夹：
   /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition

5. 点击 "Publish repository" 或 "Push origin"
6. 输入 GitHub 用户名和密码（或 Personal Access Token）

方法二：使用命令行 + Personal Access Token
───────────────────────────────────────────────────────────
1. 在 GitHub 上生成 Personal Access Token：
   - 访问：https://github.com/settings/tokens
   - 点击 "Generate new token (classic)"
   - 勾选 "repo" 权限
   - 生成并复制 token

2. 使用 token 推送：
   ```bash
   cd /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition
   
   # 使用 token 推送（替换 YOUR_TOKEN 为你的 token）
   git push https://YOUR_TOKEN@github.com/dexy1024/test_tool_for_AI.git main
   ```

方法三：配置 SSH 密钥
───────────────────────────────────────────────────────────
1. 生成 SSH 密钥：
   ```bash
   ssh-keygen -t ed25519 -C "your_email@example.com"
   ```

2. 复制公钥：
   ```bash
   cat ~/.ssh/id_ed25519.pub
   ```

3. 在 GitHub 添加 SSH 密钥：
   - 访问：https://github.com/settings/ssh/new
   - 粘贴公钥内容
   - 保存

4. 修改远程仓库地址并推送：
   ```bash
   cd /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition
   git remote set-url origin git@github.com:dexy1024/test_tool_for_AI.git
   git push -u origin main
   ```

方法四：在 GitHub 网页上传
───────────────────────────────────────────────────────────
1. 访问：https://github.com/dexy1024/test_tool_for_AI
2. 点击 "uploading an existing file"
3. 将项目文件拖拽上传
4. 提交更改

═══════════════════════════════════════════════════════════
🔧 推送后启用 GitHub Pages：
───────────────────────────────────────────────────────────

1. 访问仓库设置：
   https://github.com/dexy1024/test_tool_for_AI/settings

2. 点击左侧 "Pages"

3. 在 "Source" 部分：
   选择 "GitHub Actions"（不是 branch）
   
4. 保存设置

5. 等待自动部署：
   - 查看部署状态：https://github.com/dexy1024/test_tool_for_AI/actions
   - 部署完成后访问：https://dexy1024.github.io/test_tool_for_AI/

═══════════════════════════════════════════════════════════
📝 重要提示：
───────────────────────────────────────────────────────────

1. 不要提交 dist 文件夹（已在 .gitignore 中）
2. GitHub Actions 会自动构建 dist 文件夹
3. 部署可能需要几分钟时间
4. 如果部署失败，检查 Actions 日志

═══════════════════════════════════════════════════════════


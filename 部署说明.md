# GitHub Pages 部署说明

## ✅ 已完成的配置

1. ✅ 已配置 `vite.config.js` 的 base 路径为 `/test_tool_for_AI/`
2. ✅ 已创建 GitHub Actions 工作流 (`.github/workflows/deploy.yml`)
3. ✅ 已创建 `.gitignore` 文件
4. ✅ 已初始化 Git 仓库并提交代码

## 🚀 完成部署的步骤

### 方法一：使用 GitHub Desktop（推荐）

1. **安装 GitHub Desktop**（如果还没有）
   - 下载：https://desktop.github.com/

2. **打开项目**
   - 在 GitHub Desktop 中：File → Add Local Repository
   - 选择项目文件夹：`/Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition`

3. **推送代码**
   - 点击 "Publish repository" 或 "Push origin"
   - 输入 GitHub 用户名和密码（或 Personal Access Token）

### 方法二：使用命令行（需要配置认证）

1. **配置 GitHub 认证**

   选项A：使用 Personal Access Token（推荐）
   ```bash
   # 在 GitHub 上生成 Personal Access Token
   # Settings → Developer settings → Personal access tokens → Tokens (classic)
   # 勾选 repo 权限
   
   # 推送时使用 token
   git push https://YOUR_TOKEN@github.com/dexy1024/test_tool_for_AI.git main
   ```

   选项B：配置 SSH 密钥
   ```bash
   # 生成 SSH 密钥
   ssh-keygen -t ed25519 -C "your_email@example.com"
   
   # 将公钥添加到 GitHub
   # Settings → SSH and GPG keys → New SSH key
   
   # 修改远程仓库地址为 SSH
   git remote set-url origin git@github.com:dexy1024/test_tool_for_AI.git
   git push -u origin main
   ```

2. **推送代码**
   ```bash
   cd /Users/yuguoq/Desktop/CursorProject/Test_For_Image_Recognition
   git push -u origin main
   ```

### 方法三：在 GitHub 网页上手动上传

1. 访问：https://github.com/dexy1024/test_tool_for_AI
2. 点击 "uploading an existing file"
3. 将项目文件拖拽上传
4. 提交更改

## 📋 启用 GitHub Pages

推送代码后：

1. **进入仓库设置**
   - 访问：https://github.com/dexy1024/test_tool_for_AI/settings
   - 点击左侧 "Pages"

2. **配置部署源**
   - 在 "Source" 部分，选择 **"GitHub Actions"**
   - 保存设置

3. **等待自动部署**
   - GitHub Actions 会自动构建和部署
   - 查看部署状态：https://github.com/dexy1024/test_tool_for_AI/actions
   - 部署完成后，访问：https://dexy1024.github.io/test_tool_for_AI/

## 🌐 访问地址

部署成功后，访问地址：
```
https://dexy1024.github.io/test_tool_for_AI/
```

## ⚙️ 配置说明

- **Base Path**: `/test_tool_for_AI/`（已配置）
- **构建输出**: `dist` 文件夹
- **自动部署**: 每次推送到 `main` 分支时自动部署

## 🔍 检查部署状态

1. 查看 Actions：https://github.com/dexy1024/test_tool_for_AI/actions
2. 查看 Pages 设置：https://github.com/dexy1024/test_tool_for_AI/settings/pages

## ❓ 如果遇到问题

1. **认证失败**
   - 使用 Personal Access Token 替代密码
   - 或配置 SSH 密钥

2. **部署失败**
   - 检查 Actions 日志中的错误信息
   - 确保 `vite.config.js` 中 base 路径正确

3. **页面 404**
   - 确认 GitHub Pages 源设置为 "GitHub Actions"
   - 等待几分钟让部署完成

